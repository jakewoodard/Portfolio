
	.text

BX:  	addi $sp, $sp, -4
	sw $ra, 0($sp)
	move $t0, $a0
	move $t1, $zero
LO:	beq $t0, $0, L2
	andi $t2, $t0, 1
	beq $t2, $0, L2
	addi $t1, $t1, 1
L1:	srl $t0, $t0, 1
	b LO
L2:     move $v0, $t1
	lw $ra, 0($sp)
	addi $sp, $sp, 4
	jr $ra